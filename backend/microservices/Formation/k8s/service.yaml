apiVersion: v1
kind: Service
metadata:
  name: formation
  labels:
    app: formation
spec:
  selector:
    app: formation
  ports:
    - protocol: TCP
      port: 8096
      targetPort: 8096
  type: ClusterIP
