apiVersion: apps/v1
kind: Deployment
metadata:
  name: formation
  namespace: formation
  labels:
    app: formation
    env: production
    team: khadijaba
    version: "1.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: formation
  template:
    metadata:
      labels:
        app: formation
        env: production
        team: khadijaba
        version: "1.0"
    spec:
      containers:
        - name: formation
          image: khadijabenayed/formation:latest
          imagePullPolicy: Always
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://mysql.mysql.svc.cluster.local:3306/FormationDB?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
            - name: SPRING_DATASOURCE_USERNAME
              value: root
            - name: SPRING_DATASOURCE_PASSWORD
              value: "root"
            - name: EUREKA_CLIENT_ENABLED
              value: "false"
            - name: SPRING_CLOUD_CONFIG_ENABLED
              value: "false"
          ports:
            - containerPort: 8096
