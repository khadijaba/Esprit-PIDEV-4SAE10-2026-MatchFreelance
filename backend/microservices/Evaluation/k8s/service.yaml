apiVersion: v1
kind: Service
metadata:
  name: evaluation
  namespace: evaluation
  labels:
    app: evaluation
    env: production
    team: khadijaba
    version: "1.0"
spec:
  selector:
    app: evaluation
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
  type: ClusterIP
