# Profil test : MySQL identique à application.properties (jdbc URL, user, mot de passe).
# Démarrer MySQL (base skill) avant mvn test / mvn install.
# Eureka et Config Server non requis pour ce test.

eureka.client.enabled=false
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

spring.cloud.config.enabled=false
spring.cloud.discovery.enabled=false
