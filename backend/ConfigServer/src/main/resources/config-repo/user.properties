# Configuration pour le service USER (spring.application.name=USER)
# Exemple : eureka, JWT, etc. (décommenter si centralisation souhaitée)
# eureka.client.service-url.defaultZone=http://localhost:8761/eureka
# server.port=8085
# server.servlet.context-path=/api
