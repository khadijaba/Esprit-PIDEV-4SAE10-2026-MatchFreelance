# USER — spring.application.name=USER (chargé en plus de application.properties du dépôt)
# Les secrets (jwt.secret) restent en local ou variables d'environnement.

jwt.expiration-ms=86400000

logging.level.esprit.user=INFO
