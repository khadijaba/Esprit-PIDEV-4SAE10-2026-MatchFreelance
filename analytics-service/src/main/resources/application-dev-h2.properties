# Local smoke-test without PostgreSQL (CI / laptop without Docker PG)
spring.cloud.config.enabled=false

spring.r2dbc.url=r2dbc:h2:mem:///matchfreelance_analytics;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
spring.r2dbc.username=sa
spring.r2dbc.password=
