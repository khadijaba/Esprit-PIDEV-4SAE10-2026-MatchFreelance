<<<<<<< HEAD
# Configuration partagée par tous les microservices (Config Server - File Store)
# Les services chargent ce fichier + leur propre {application}.properties

# Eureka (commun)
eureka.client.service-url.defaultZone=${EUREKA_URL:http://localhost:8761/eureka/}
eureka.instance.prefer-ip-address=true

# Logging
logging.level.org.springframework.cloud.config=INFO
=======
# Configuration commune chargée pour tous les clients (Spring Cloud Config)
# + fichier spécifique {spring.application.name}.properties
#
# MySQL (fichiers par service) : jdbc:mysql://localhost:3306/... , root , mot de passe vide — typique XAMPP.

# Eureka (commun)
eureka.client.service-url.defaultZone=${EUREKA_URL:http://localhost:8761/eureka/}
eureka.instance.prefer-ip-address=true
# Heartbeats Netflix uniquement (pas /actuator/health) — évite des DOWN erronés en Docker si un indicateur de santé fluctue.
eureka.client.healthcheck.enabled=false

logging.level.org.springframework.cloud.config=INFO
>>>>>>> 8d5250d (Ajout du projet MatchFreelance)
