# Mot de passe aligné sur BackEnd/infra/k8s/mysql/mysql.yaml (Secret mysql-root, clé root-password).
# Dupliqué dans le namespace project car Kubernetes n’autorise pas secretKeyRef cross-namespace.
apiVersion: v1
kind: Secret
metadata:
  name: project-db-credentials
  namespace: project
  labels:
    app.kubernetes.io/name: project-ms
    app.kubernetes.io/component: database-client
    app.kubernetes.io/part-of: matchfreelance
    env: dev
    team: backend
type: Opaque
stringData:
  username: root
  password: changeme
