# Eureka : nom = lb://CANDIDATURE dans la Gateway
spring.application.name=CANDIDATURE
server.port=8087

spring.config.import=optional:configserver:http://localhost:8888

eureka.client.service-url.defaultZone=${EUREKA_URL:http://localhost:8761/eureka/}
eureka.instance.prefer-ip-address=true

spring.profiles.active=mysql

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Si les candidatures affichent toujours 0 entretien / pas eligible : le MS Candidature n atteint pas INTERVIEW via Eureka.
# En dev sans decouverte, decommenter (adapter le port) :
# spring.cloud.openfeign.client.config.INTERVIEW.url=http://localhost:8089
