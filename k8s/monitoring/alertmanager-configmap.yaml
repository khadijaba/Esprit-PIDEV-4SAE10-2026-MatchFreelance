apiVersion: v1
kind: ConfigMap
metadata:
  name: alertmanager-config
  namespace: matchfreelance
data:
  alertmanager.yml: |
    route:
      receiver: default
      group_wait: 10s
      group_interval: 30s
      repeat_interval: 2h
    receivers:
      - name: default
