apiVersion: v1
kind: Service
metadata:
  name: contract-service
  namespace: matchfreelance
  labels:
    app: contract
spec:
  selector:
    app: contract
  ports:
    - name: http
      port: 8088
      targetPort: 8088
  type: ClusterIP
